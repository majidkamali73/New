# اپلیکیشن محاسبه مساحت مثلث

این یک پروژه‌ی کامل اندروید (Kotlin) است که مساحت مثلث را در سه حالت محاسبه می‌کند:

1. **معلوم بودن هر سه ضلع** — با فرمول هرون
2. **معلوم بودن یک ضلع و دو زاویه** — با استفاده از قانون سینوس‌ها
3. **معلوم بودن دو ضلع و زاویه بین آن‌ها** — با فرمول (۱/۲) × a × b × sin(C)

## نحوه اجرا

### روش ۱: باز کردن در Android Studio (پیشنهادی)
1. [Android Studio](https://developer.android.com/studio) را نصب کنید (اگر ندارید).
2. کل پوشه `TriangleAreaApp` را باز کنید: File → Open → پوشه پروژه را انتخاب کنید.
3. صبر کنید تا Gradle همگام‌سازی (Sync) شود — نیاز به اینترنت دارد تا وابستگی‌ها دانلود شوند.
4. یک دستگاه اندرویدی وصل کنید یا یک شبیه‌ساز (Emulator) بسازید.
5. روی دکمه ▶ Run کلیک کنید.

### روش ۲: ساخت فایل APK از خط فرمان
اگر Android SDK و Gradle نصب دارید:
```
cd TriangleAreaApp
./gradlew assembleDebug
```
فایل خروجی در مسیر زیر ساخته می‌شود:
```
app/build/outputs/apk/debug/app-debug.apk
```
این فایل را می‌توانید مستقیماً روی گوشی اندرویدی نصب کنید.

## ساختار پروژه
```
TriangleAreaApp/
├── app/
│   ├── build.gradle                 ← تنظیمات ماژول اپ
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/example/trianglearea/
│       │   └── MainActivity.kt      ← تمام منطق محاسبات اینجاست
│       └── res/
│           ├── layout/activity_main.xml   ← رابط کاربری
│           └── values/                    ← رنگ‌ها، متن‌ها، تم
├── build.gradle                     ← تنظیمات کلی پروژه
└── settings.gradle
```

## نکات فنی
- حداقل نسخه اندروید پشتیبانی‌شده: Android 5.0 (API 21)
- زبان: Kotlin
- در حالت ۱، اعتبارسنجی نامساوی مثلثی (Triangle Inequality) انجام می‌شود.
- در حالت ۲ و ۳ ورودی زاویه‌ها بر حسب **درجه** است (نه رادیان).
- اگر بخواهید بعداً اپ را شخصی‌سازی کنید (رنگ، آیکون، فونت فارسی و ...)، فایل‌های
  `res/values/themes.xml` و `res/layout/activity_main.xml` نقطه‌ی شروع خوبی هستند.

موفق باشید! اگر خواستید فیچر جدیدی (مثل ذخیره تاریخچه محاسبات یا محاسبه محیط مثلث) اضافه شود،
فقط بگویید تا برایتان اضافه کنم.
